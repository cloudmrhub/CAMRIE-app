# Core AWS + HTTP
boto3
requests

# MRI pipeline dependencies
numpy
SimpleITK
matplotlib
h5py
scipy

# Pulseq reader
pypulseq

# vtk (optional, used by some helpers)
vtk

# CloudMR utilities (pynico-eros-montin is brought in by cmtools)
# pynico_eros_montin still imports pkg_resources, which setuptools 81+ removed.
setuptools<81
cmtools @ git+https://github.com/cloudmrhub/cloudmr-tools.git@v1.1
