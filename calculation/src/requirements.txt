# Core AWS + HTTP
boto3
requests

# MRI pipeline dependencies
numpy
SimpleITK
matplotlib
h5py
scipy

# Pulseq reader
pypulseq

# vtk (optional, used by some helpers)
vtk

# CloudMR utilities (pynico-eros-montin is brought in by cmtools@v2)
cmtools @ git+https://github.com/cloudmrhub/cloudmr-tools.git
